# Meta Marketing API
# Use a SYSTEM USER token, not a short-lived user token. Short-lived tokens
# expire mid-backfill and surface as error code 190.
META_ACCESS_TOKEN=
META_API_VERSION=v21.0

# GCP
GCP_PROJECT=
BQ_DATASET=marketing
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
